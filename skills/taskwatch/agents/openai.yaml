interface:
  display_name: "TaskWatch"
  short_description: "Scaffold a Linux job monitor or cross-platform Codex goal-status hook."
  default_prompt: "Set up a Linux workspace monitor for a long-running job, or install the cross-platform global TaskWatch Stop hook for goal terminal email notification."
