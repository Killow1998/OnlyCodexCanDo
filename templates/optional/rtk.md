## Optional RTK Usage

Select this module only after explaining that filtered output can save context but may hide diagnostic evidence.

- Use `rtk` selectively when filtering saves context without hiding required evidence.
- Rerun the native command whenever complete output is needed for diagnosis or verification. Do not let token savings reduce capability or confidence.
