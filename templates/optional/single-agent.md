## Single-Agent Default

Select this module when the user prefers to avoid coordination overhead and duplicated context.

- Default to one agent. Use subagents only when the user explicitly authorizes them and the work is genuinely independent.
- Pass only the minimum necessary context to a subagent; do not copy full histories or images by default.
