## Current-Checkout Workflow

Select this module when the user's environment, dependencies, generated state, or personal workflow should remain in one checkout.

- Work in the current checkout. Do not create or use Git worktrees unless the user explicitly requests them.
